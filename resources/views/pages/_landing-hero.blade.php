<section class="rw-landing-hero" aria-labelledby="landing-hero-title">
    <div class="container rw-landing-hero__inner">
        <span class="rw-section-label rw-section-label--hero">{{ $eyebrow }}</span>
        <h1 id="landing-hero-title">{{ $heading }}</h1>
        <p class="rw-landing-hero__lead">{{ $lead }}</p>
    </div>
</section>
